SSLSessionCache shmcb:logs/ssl_scache(512000)
SSLSessionCacheTimeout 300

Listen 443

<VirtualHost *:443>
    ServerName app.louisplace.com
    ServerAlias www.app.louisplace.com

    # SSL Configuration
    SSLEngine on
    SSLCertificateFile /etc/apache2/certs/cloudflare-origin.pem
    SSLCertificateKeyFile /etc/apache2/certs/cloudflare-origin.key

    # HSTS Header
    Header always set Strict-Transport-Security "max-age=15552000; includeSubDomains; preload"
    
    # Proxy configuration
    ProxyPreserveHost On
    ProxyPass / http://client:3000/
    ProxyPassReverse / http://client:3000/
    
    ErrorLog logs/error.log
    CustomLog logs/access.log combined
</VirtualHost>

